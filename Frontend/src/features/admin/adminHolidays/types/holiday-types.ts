export interface Holiday {
    id: string;
    branch_id: string;
    date: string;
    name: string;
}

export interface HolidayFormData {
    branch_id: string;
    date: string;
    name: string;
}